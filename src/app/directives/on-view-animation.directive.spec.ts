import { OnViewAnimationDirective } from './on-view-animation.directive';

describe('OnViewAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new OnViewAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
